{
  "name": "resumix",
  "version": "1.0.0",
  "description": "Resumix — AI Resume Builder (React + Vite + Express + Claude)",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npm run install:all && concurrently -n server,client -c cyan,magenta \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
